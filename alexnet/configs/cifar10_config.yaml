defaults:
  - dataset: cifar10
  - model: cifar10_small
  - optimizer: sgd
  - trainer: default
  - _self_

run_name: "large-batch-training"
trainer:
  max_epochs: 10
model:
  pool:
    type: "max"

optimizer:
  lr: 0.01
dataset:
  batch_size: 128
seed: 42