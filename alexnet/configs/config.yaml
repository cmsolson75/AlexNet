defaults:
  - dataset: cifar100
  - model: cifar100
  - optimizer: sgd
  - trainer: default
  - checkpoint: default
  - _self_

run_name: "first-test"
checkpoint:
  resume:
    ckpt_path: /home/cameronolson/documents/AlexNet/cifar100-alexnet/gvkv02pt/checkpoints/last.ckpt
project: "cifar100-alexnet"
trainer:
  max_epochs: 100
optimizer:
  lr: 0.01
dataset:
  batch_size: 256

seed: 42